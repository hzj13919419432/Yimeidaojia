apiready = function(){
    api.parseTapmode();
    init();


}
function init() {
  var safeArea = api.safeArea;
  //$('body>header').css('padding-top',api.safeArea.top + 'px');
  var header = document.querySelector('body>header');
  $api.fixStatusBar(header);
}
